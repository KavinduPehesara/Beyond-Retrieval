# Data extraction demo — van_der_Valk_2021, week of 20 September 2026. Runs
# over this review's own screening run's verified-include records (11 —
# small, since verification rate on this review was the lowest of the three
# screened so far at 39.3%).
#
# Before running:
#   1. Ollama running locally, qwen2.5:7b-instruct pulled (already true).
#   2. Commit, then run with --require-clean.

name: extract_demo_valk2021

source_run_id: 20260920T131834130857Z-89b8a49fec
review: van_der_Valk_2021
max_records: null   # every verified-include record from the source run

provider: ollama
model: qwen2.5:7b-instruct
prompt_version: extract_v1
temperature: 0.0
max_output_tokens: 512
max_retries: 3
seed: 42

ceiling_usd: 0.00
usd_per_1m_input: 0.0    # local GPU inference; no per-token API cost
usd_per_1m_output: 0.0

db_path: data/slr.db
runs_dir: runs
prompts_dir: prompts
cache_enabled: true
