# Week 11 — gap-statement discovery, Nelson_2002. Runs over this review's
# 114 verified-include records (same source run extract_demo.yaml used),
# asking whether each abstract states a research gap in the authors' own
# words. Local Ollama, $0.
#
# Before running:
#   1. Ollama running locally, qwen2.5:7b-instruct pulled (already true).
#   2. Commit, then run with --require-clean.

name: gap_demo_nelson2002

source_run_id: 20260918T060412811907Z-2556bbe54c
review: Nelson_2002
max_records: null

provider: ollama
model: qwen2.5:7b-instruct
prompt_version: gap_v1
temperature: 0.0
max_output_tokens: 512
max_retries: 3
seed: 42

ceiling_usd: 0.00
usd_per_1m_input: 0.0
usd_per_1m_output: 0.0

db_path: data/slr.db
runs_dir: runs
prompts_dir: prompts
cache_enabled: true
